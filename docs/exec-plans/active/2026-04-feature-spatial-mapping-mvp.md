# 计划 3：空间映射与配件推荐服务 (Service)

## 目标
实现 2D/3D 空间结构的逻辑计算，以及基于谷子物理尺寸的配件智能推荐。

## 验收标准
- [ ] 完成 `src/types/spatial.ts` 的树状容器模型（支持 `x, y, z` 及 `w, h, d`）。
- [ ] 编写 `src/service/accessory.service.ts`，能够根据输入的谷子分类与尺寸，自动返回推荐的收纳配件（例如 63*88 的卡片推荐 65*90 的自封袋）。
